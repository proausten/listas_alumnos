import { ListaAlumnoDirective } from './lista-alumno.directive';

describe('ListaAlumnoDirective', () => {
  it('should create an instance', () => {
    const directive = new ListaAlumnoDirective();
    expect(directive).toBeTruthy();
  });
});
